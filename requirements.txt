# Ranges, not hard pins. A hard pin on playwright transitively pinned
# greenlet==3.0.3, which has no wheel for Python 3.13 and forced a source
# build needing MSVC. greenlet only gained cp313 wheels at 3.1.0.
playwright>=1.49,<2
requests>=2.32,<3
PyYAML>=6.0,<7
pytest>=8.3,<9
